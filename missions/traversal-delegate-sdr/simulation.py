# IS_SIMULATION denotes whether the mission is running as a simulation or not.
# Change it to true to allow simulating the mission without an SDR, but leave it as False in git
# It should only be true when running missions on your local machine and not while running missions on real-time drones
IS_SIMULATION = False   # Don't commit as True
TARGET_LOCATION = 29.716326, -95.409311
